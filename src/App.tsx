import React from "react";
import { Button } from "./components/Button";

function App() {
  return (
    <div className="App">
      <Button value="ボタン" onClick={() => {}} />
    </div>
  );
}

export default App;
